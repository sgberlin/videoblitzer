{
  "name": "@videoblitzer/desktop-recorder",
  "version": "0.1.0",
  "description": "Production desktop recorder for VideoBlitzer uploads and local capture workflows.",
  "author": "VideoBlitzer",
  "private": true,
  "type": "commonjs",
  "main": "dist/bootstrap.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "tsc && tsc -p tsconfig.renderer.json && node scripts/copy-assets.mjs",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "smoke:navigation": "node scripts/smoke-navigation.mjs",
    "package:mac": "npm run build && electron-builder --mac dmg --x64 --arm64 --config electron-builder.json",
    "package:win": "npm run build && electron-builder --win nsis --x64 --config electron-builder.json"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  },
  "devDependencies": {
    "electron": "^37.10.3",
    "electron-builder": "^25.1.8",
    "tsx": "latest",
    "typescript": "latest"
  }
}
