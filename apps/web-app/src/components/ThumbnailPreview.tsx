import { thumbnailQualityChecklist, thumbnailTemplates } from "@videoblitzer/thumbnail-engine";
export function ThumbnailPreview() { return <div className="grid grid-2"><div className="card"><h3>Thumbnail Builder</h3><p>Default identity: team colors + initials + player/game frames.</p><select className="input">{thumbnailTemplates.map((template) => <option key={template}>{template}</option>)}</select><div className="hero" style={{ marginTop: 16 }}><h2>VB HIGHLIGHTS</h2><p className="muted">YouTube 1280x720 · Shorts 1080x1920 · Square 1080x1080</p></div></div><div className="card"><h3>Quality Checklist</h3>{thumbnailQualityChecklist.map((item) => <p key={item}>□ {item}</p>)}</div></div>; }
