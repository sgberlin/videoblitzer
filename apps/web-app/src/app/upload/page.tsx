export default function UploadPage() { return <section className="hero"><h1>Upload Existing Video</h1><p className="muted">Accepted formats: mp4, mov, mkv, webm. Uploads use signed Cloudflare R2 URLs generated by the API, not Vercel serverless payloads.</p><div className="grid grid-2"><div className="card"><h3>Upload Existing Video</h3><input className="input" type="file" accept=".mp4,.mov,.mkv,.webm,video/mp4,video/quicktime,video/x-matroska,video/webm" /><p className="muted">After upload, VideoBlitzer creates the project and video record, then queues analysis.</p><button className="button">Prepare signed upload</button></div><div className="card"><h3>Record New Match</h3><p className="muted">Coming desktop app. Use replay buffers, hotkeys, and separate tracks, then upload to the dashboard.</p><button className="button secondary">Coming Desktop App</button></div></div></section>; }
